import React from 'react';
import SectionPageContainer from '../../../components/TeacherPageComponent/SectionPageContainer';

function Section() {
      return (
            <div>
                  <SectionPageContainer />
            </div>
      );
}

export default Section;
