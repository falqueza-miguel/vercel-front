import React from 'react';
import SchedulePageContainer from '../../components/TeacherPageComponent/SchedulePageContainer';
function Schedule() {
      return (
            <div>
                  <SchedulePageContainer />
            </div>
      );
}

export default Schedule;
