import React from 'react';
import SchedulePageContainer from '../../components/StudentPageComponent/SchedulePageContainer';
function Schedule() {
      return (
            <div>
                  <SchedulePageContainer />
            </div>
      );
}

export default Schedule;
