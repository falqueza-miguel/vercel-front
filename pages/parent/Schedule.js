import React from 'react';
import SchedulePageContainer from '../../components/ParentPageComponent/SchedulePageContainer';
function Schedule() {
      return (
            <div>
                  <SchedulePageContainer />
            </div>
      );
}

export default Schedule;
